= link_to 'New Cat', new_cat_path
List of Cats
- @cats.each do |cat|
  = link_to "Name: #{cat.name}", cat_path(cat)
  = "Age: #{cat.age} years old"
  = "Friendly: #{cat.friendly}"
  = link_to 'Edit', edit_cat_path(cat)
  = link_to 'Delete', cat_path(cat), method: :delete
